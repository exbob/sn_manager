# 应用图标 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 sn-manager 提供圆形橙色 SN 字母标，覆盖运行时窗口/任务栏图标与 Windows `sn-manager.exe` 文件图标。

**Architecture:** 在 `assets/icons/` 入库 SVG/PNG/ICO。`resolve_app_icon_path()`（`paths.py`）按 frozen `_MEIPASS` 与开发态向上查找解析 PNG；`__main__.py` 在创建 `QApplication` 后 `setWindowIcon`。`build.sh`、Windows CI 与 `sn-manager.spec` 增加 `--icon` 与 `--add-data assets/icons`。

**Tech Stack:** Python 3.12、PySide6、PyInstaller、Pillow（仅一次性生成 PNG/ICO，不写入项目依赖）、Bash、GitHub Actions。

## Global Constraints

- 视觉：圆形底 `#EA580C`，字 `#FFFFFF`，透明画布，无描边；字母 **SN**
- 运行时只用 `sn-manager.png`；`.ico` 仅给 PyInstaller `--icon`
- 缺失图标时静默跳过，不弹窗、不抛错
- `--add-data` 固定：`assets/icons` → `assets/icons`（Linux `:` / Windows `;`）
- frozen 路径：`Path(sys._MEIPASS) / "assets" / "icons" / "sn-manager.png"`
- 规格：`docs/superpowers/specs/2026-08-02-app-icon-design.md`
- 不引入 Qt `.qrc`；不改业务功能 / 窗口标题 / 整体主题

## File Structure

| 文件 | 职责 |
| ---- | ---- |
| `assets/icons/sn-manager.svg` | 矢量源 |
| `assets/icons/sn-manager.png` | 256×256 运行时图标 |
| `assets/icons/sn-manager.ico` | 多尺寸 Windows exe 图标 |
| `scripts/export-icon.sh` | 可选：用 Pillow 从绘制逻辑导出 PNG/ICO |
| `src/sn_manager/app/paths.py` | `resolve_app_icon_path() -> Path \| None` |
| `tests/test_paths.py` | 图标路径解析单测（追加） |
| `src/sn_manager/app/__init__.py` | 导出 `resolve_app_icon_path` |
| `src/sn_manager/__main__.py` | `setWindowIcon` |
| `scripts/build.sh` | `--icon` + `--add-data` |
| `.github/workflows/release-windows-gui.yml` | 同上（Windows 分隔符） |
| `sn-manager.spec` | `icon=` + `datas` |

---

### Task 1: 图标资源（SVG + PNG + ICO）

**Files:**
- Create: `assets/icons/sn-manager.svg`
- Create: `assets/icons/sn-manager.png`
- Create: `assets/icons/sn-manager.ico`
- Create: `scripts/export-icon.sh`

**Interfaces:**
- Consumes: 无
- Produces: 仓库内三份图标文件；`scripts/export-icon.sh` 可重复导出 PNG/ICO

- [ ] **Step 1: 写入 SVG 源文件**

创建目录 `assets/icons/`，写入 `assets/icons/sn-manager.svg`：

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 256 256" width="256" height="256">
  <circle cx="128" cy="128" r="114" fill="#EA580C"/>
  <text
    x="128"
    y="128"
    text-anchor="middle"
    dominant-baseline="central"
    font-family="Segoe UI, Arial, Helvetica, sans-serif"
    font-size="96"
    font-weight="700"
    fill="#FFFFFF"
    letter-spacing="4"
  >SN</text>
</svg>
```

- [ ] **Step 2: 写入导出脚本并生成 PNG/ICO**

创建 `scripts/export-icon.sh`：

```bash
#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

uv run --with pillow python - <<'PY'
from pathlib import Path

from PIL import Image, ImageDraw, ImageFont

OUT = Path("assets/icons")
OUT.mkdir(parents=True, exist_ok=True)
ORANGE = (234, 88, 12, 255)  # #EA580C
WHITE = (255, 255, 255, 255)


def render(size: int) -> Image.Image:
    img = Image.new("RGBA", (size, size), (0, 0, 0, 0))
    draw = ImageDraw.Draw(img)
    margin = int(size * 0.055)
    draw.ellipse((margin, margin, size - 1 - margin, size - 1 - margin), fill=ORANGE)
    # Prefer a bold TrueType; fall back to default bitmap font.
    font = None
    for name in (
        "/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf",
        "/usr/share/fonts/truetype/liberation/LiberationSans-Bold.ttf",
        "C:/Windows/Fonts/arialbd.ttf",
        "C:/Windows/Fonts/segoeuib.ttf",
    ):
        p = Path(name)
        if p.is_file():
            font = ImageFont.truetype(str(p), size=max(10, int(size * 0.38)))
            break
    if font is None:
        font = ImageFont.load_default()
    text = "SN"
    bbox = draw.textbbox((0, 0), text, font=font)
    tw, th = bbox[2] - bbox[0], bbox[3] - bbox[1]
    x = (size - tw) / 2 - bbox[0]
    y = (size - th) / 2 - bbox[1]
    draw.text((x, y), text, font=font, fill=WHITE)
    return img


base = render(256)
base.save(OUT / "sn-manager.png", format="PNG")
sizes = [(16, 16), (32, 32), (48, 48), (256, 256)]
images = [render(s) for s, _ in sizes]
images[0].save(
    OUT / "sn-manager.ico",
    format="ICO",
    sizes=sizes,
    append_images=images[1:],
)
print(f"Wrote {OUT / 'sn-manager.png'} and {OUT / 'sn-manager.ico'}")
PY
```

赋予可执行权限并运行：

```bash
chmod +x scripts/export-icon.sh
./scripts/export-icon.sh
```

Expected: 打印 Wrote …；`assets/icons/` 下存在 `sn-manager.png` 与 `sn-manager.ico`。

- [ ] **Step 3: 快速目视检查**

```bash
ls -la assets/icons/
file assets/icons/sn-manager.png assets/icons/sn-manager.ico assets/icons/sn-manager.svg
```

Expected: PNG 为 256×256 PNG；ICO 为 MS Windows icon；SVG 为 XML/SVG。

- [ ] **Step 4: Commit**

```bash
git add assets/icons/sn-manager.svg assets/icons/sn-manager.png assets/icons/sn-manager.ico scripts/export-icon.sh
git commit -m "$(cat <<'EOF'
feat(assets): add SN circular orange app icon

EOF
)"
```

---

### Task 2: `resolve_app_icon_path`（TDD）

**Files:**
- Modify: `src/sn_manager/app/paths.py`
- Modify: `tests/test_paths.py`
- Modify: `src/sn_manager/app/__init__.py`

**Interfaces:**
- Consumes: `sys.frozen`、`sys._MEIPASS`（frozen）；`Path.cwd()` / `__file__` 向上查找（开发态）
- Produces: `resolve_app_icon_path() -> Path | None`

- [ ] **Step 1: 写失败单测**

在 `tests/test_paths.py` 追加：

```python
from sn_manager.app.paths import resolve_app_icon_path


def test_resolve_app_icon_frozen_meipass(tmp_path: Path, monkeypatch) -> None:
    icon_dir = tmp_path / "assets" / "icons"
    icon_dir.mkdir(parents=True)
    icon = icon_dir / "sn-manager.png"
    icon.write_bytes(b"\x89PNG\r\n\x1a\n")
    monkeypatch.setattr(paths_mod.sys, "frozen", True, raising=False)
    monkeypatch.setattr(paths_mod.sys, "_MEIPASS", str(tmp_path), raising=False)
    assert resolve_app_icon_path() == icon.resolve()


def test_resolve_app_icon_frozen_missing(tmp_path: Path, monkeypatch) -> None:
    monkeypatch.setattr(paths_mod.sys, "frozen", True, raising=False)
    monkeypatch.setattr(paths_mod.sys, "_MEIPASS", str(tmp_path), raising=False)
    assert resolve_app_icon_path() is None


def test_resolve_app_icon_dev_from_cwd(tmp_path: Path, monkeypatch) -> None:
    icon_dir = tmp_path / "assets" / "icons"
    icon_dir.mkdir(parents=True)
    icon = icon_dir / "sn-manager.png"
    icon.write_bytes(b"\x89PNG\r\n\x1a\n")
    monkeypatch.setattr(paths_mod.sys, "frozen", False, raising=False)
    if hasattr(paths_mod.sys, "_MEIPASS"):
        monkeypatch.delattr(paths_mod.sys, "_MEIPASS", raising=False)
    monkeypatch.chdir(tmp_path)
    assert resolve_app_icon_path() == icon.resolve()


def test_resolve_app_icon_dev_walk_from_module(tmp_path: Path, monkeypatch) -> None:
    root = tmp_path / "repo"
    icon_dir = root / "assets" / "icons"
    icon_dir.mkdir(parents=True)
    icon = icon_dir / "sn-manager.png"
    icon.write_bytes(b"\x89PNG\r\n\x1a\n")
    pkg = root / "src" / "sn_manager" / "app"
    pkg.mkdir(parents=True)
    monkeypatch.setattr(paths_mod.sys, "frozen", False, raising=False)
    if hasattr(paths_mod.sys, "_MEIPASS"):
        monkeypatch.delattr(paths_mod.sys, "_MEIPASS", raising=False)
    monkeypatch.setattr(paths_mod, "__file__", str(pkg / "paths.py"))
    monkeypatch.chdir(tmp_path)  # cwd 无图标，须向上找到 repo
    assert resolve_app_icon_path() == icon.resolve()


def test_resolve_app_icon_dev_missing(tmp_path: Path, monkeypatch) -> None:
    monkeypatch.setattr(paths_mod.sys, "frozen", False, raising=False)
    if hasattr(paths_mod.sys, "_MEIPASS"):
        monkeypatch.delattr(paths_mod.sys, "_MEIPASS", raising=False)
    monkeypatch.setattr(paths_mod, "__file__", str(tmp_path / "paths.py"))
    monkeypatch.chdir(tmp_path)
    assert resolve_app_icon_path() is None
```

同时更新文件顶部 import：确保 `resolve_app_icon_path` 从 `sn_manager.app.paths` 导入（与现有 `app_dir` 等并列）。

- [ ] **Step 2: 运行单测确认失败**

```bash
cd /home/lsc/workspace/sn-manager
uv run pytest tests/test_paths.py -k resolve_app_icon -v
```

Expected: FAIL（`ImportError` 或 `resolve_app_icon_path` 未定义）。

- [ ] **Step 3: 实现 `resolve_app_icon_path`**

在 `src/sn_manager/app/paths.py` 增加常量与函数（紧挨现有 `_MAX_WALK_UP` / `resolve_user_manual_path` 风格）：

```python
_APP_ICON_REL = Path("assets") / "icons" / "sn-manager.png"


def resolve_app_icon_path() -> Path | None:
    """返回应用图标 PNG 路径；找不到则 None。"""
    if getattr(sys, "frozen", False):
        meipass = getattr(sys, "_MEIPASS", None)
        if not meipass:
            return None
        candidate = Path(meipass) / _APP_ICON_REL
        return candidate.resolve() if candidate.is_file() else None

    candidates: list[Path] = [Path.cwd() / _APP_ICON_REL]
    here = Path(__file__).resolve().parent
    for i, parent in enumerate([here, *here.parents]):
        if i > _MAX_WALK_UP:
            break
        path = parent / _APP_ICON_REL
        if path not in candidates:
            candidates.append(path)
    for path in candidates:
        if path.is_file():
            return path.resolve()
    return None
```

在 `src/sn_manager/app/__init__.py` 导出：

```python
from sn_manager.app.paths import (
    default_db_path,
    resolve_app_icon_path,
    resolve_user_manual_path,
)
# __all__ 增加 "resolve_app_icon_path"
```

- [ ] **Step 4: 运行单测确认通过**

```bash
uv run pytest tests/test_paths.py -k resolve_app_icon -v
```

Expected: PASS（5 passed）。

- [ ] **Step 5: Commit**

```bash
git add src/sn_manager/app/paths.py src/sn_manager/app/__init__.py tests/test_paths.py
git commit -m "$(cat <<'EOF'
feat(app): resolve packaged and dev app icon path

EOF
)"
```

---

### Task 3: `__main__.py` 设置窗口图标

**Files:**
- Modify: `src/sn_manager/__main__.py`

**Interfaces:**
- Consumes: `resolve_app_icon_path() -> Path | None`
- Produces: 启动时 `QApplication.setWindowIcon`（有路径时）

- [ ] **Step 1: 接入 `setWindowIcon`**

修改 `src/sn_manager/__main__.py`：

```python
import sys

from PySide6.QtCore import Qt
from PySide6.QtGui import QIcon
from PySide6.QtWidgets import QApplication, QMessageBox

from sn_manager.app.paths import default_db_path, resolve_app_icon_path
from sn_manager.app.services import SnService
from sn_manager.db.connection import connect
from sn_manager.gui.fonts import apply_ui_font
from sn_manager.gui.main_window import MainWindow


def main() -> None:
    app = QApplication(sys.argv)
    # 固定浅色，避免跟随 Windows 系统深色模式
    app.styleHints().setColorScheme(Qt.ColorScheme.Light)
    icon_path = resolve_app_icon_path()
    if icon_path is not None:
        app.setWindowIcon(QIcon(str(icon_path)))
    chosen = apply_ui_font(app)
    # ... 其余保持不变 ...
```

- [ ] **Step 2: 冒烟（开发态）**

```bash
uv run python -c "from sn_manager.app.paths import resolve_app_icon_path; p=resolve_app_icon_path(); print(p); assert p is not None and p.is_file()"
```

Expected: 打印仓库内 `.../assets/icons/sn-manager.png`。

可选手工：`uv run python -m sn_manager`，确认窗口标题栏图标为橙色 SN 圆标。

- [ ] **Step 3: Commit**

```bash
git add src/sn_manager/__main__.py
git commit -m "$(cat <<'EOF'
feat(gui): apply app window icon at startup

EOF
)"
```

---

### Task 4: 打包与 CI 接入

**Files:**
- Modify: `scripts/build.sh`
- Modify: `.github/workflows/release-windows-gui.yml`
- Modify: `sn-manager.spec`

**Interfaces:**
- Consumes: `assets/icons/sn-manager.ico`、`assets/icons/` 目录
- Produces: 带 exe 图标与 onedir 内 PNG 资源的构建命令

- [ ] **Step 1: 更新 `scripts/build.sh`**

完整文件应为：

```bash
#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

./scripts/git-version.sh > app_version.txt

uv run pyinstaller \
  --noconfirm \
  --windowed \
  --name sn-manager \
  --paths src \
  --collect-submodules PySide6 \
  --icon assets/icons/sn-manager.ico \
  --add-data "app_version.txt:." \
  --add-data "assets/icons:assets/icons" \
  src/sn_manager/__main__.py

cp -f docs/user-manual.md dist/sn-manager/user-manual.md

echo "Built: dist/sn-manager/sn-manager"
```

- [ ] **Step 2: 更新 Windows workflow**

将 `.github/workflows/release-windows-gui.yml` 中 Build executable 步骤改为：

```yaml
      - name: Build executable (PyInstaller onedir)
        shell: pwsh
        run: |
          uv run pyinstaller `
            --noconfirm `
            --windowed `
            --name sn-manager `
            --paths src `
            --collect-submodules PySide6 `
            --icon assets/icons/sn-manager.ico `
            --add-data "app_version.txt;." `
            --add-data "assets/icons;assets/icons" `
            src/sn_manager/__main__.py
```

- [ ] **Step 3: 更新 `sn-manager.spec`**

在 `Analysis(...)` 中：

```python
datas=[('assets/icons', 'assets/icons'), ('app_version.txt', '.')],
```

（若 `app_version.txt` 仅由 CLI 构建生成、spec 不常用，至少保证 icons；若原先 `datas=[]`，改为包含 icons；`app_version.txt` 仅在文件存在时需要——与现有 CLI 构建对齐时，icons 必须有。）

在 `EXE(...)` 中增加：

```python
    icon='assets/icons/sn-manager.ico',
```

完整 `EXE` 相关片段示例（保留其余原有参数）：

```python
exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='sn-manager',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='assets/icons/sn-manager.ico',
)
```

并将 `Analysis` 的 `datas` 设为：

```python
    datas=[('assets/icons', 'assets/icons')],
```

- [ ] **Step 4: 本地构建冒烟（可选，Linux）**

```bash
./scripts/build.sh
test -f dist/sn-manager/assets/icons/sn-manager.png
```

Expected: 构建成功；onedir 内存在 PNG。

- [ ] **Step 5: 跑相关单测**

```bash
uv run pytest tests/test_paths.py -v
```

Expected: 全部 PASS。

- [ ] **Step 6: Commit**

```bash
git add scripts/build.sh .github/workflows/release-windows-gui.yml sn-manager.spec
git commit -m "$(cat <<'EOF'
build: embed app icon in PyInstaller and Windows CI

EOF
)"
```

---

## Spec Coverage Checklist

| 规格要求 | 任务 |
| -------- | ---- |
| SVG/PNG/ICO 入库，圆形橙底白字 SN | Task 1 |
| `resolve_app_icon_path`，frozen/dev，缺失返回 None | Task 2 |
| `__main__.py` `setWindowIcon`，缺失静默跳过 | Task 3 |
| `build.sh` / CI / spec：`--icon` + `--add-data assets/icons` | Task 4 |
| 路径解析单测 | Task 2 |
| 不引入 `.qrc` / 不改业务 | 全任务遵守 |

## Self-Review Notes

- 无 TBD/占位；函数名与规格一致：`resolve_app_icon_path() -> Path | None`
- Pillow 仅用于 `export-icon.sh` 一次性/`--with`，不写入 `pyproject.toml`
- ICO 的 `append_images` 写法以 Pillow 文档为准；若某版本保存失败，改为先保存各尺寸再 `images[0].save(..., append_images=images[1:])`（同尺寸列表）
